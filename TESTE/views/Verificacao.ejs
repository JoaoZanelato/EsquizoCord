<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %> - EsquizoCord</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        .verification-container {
            background-color: var(--background-secondary);
            padding: 40px;
            border-radius: 8px;
            text-align: center;
            max-width: 450px;
            width: 90%;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }
        .verification-icon {
            font-size: 5rem;
            margin-bottom: 20px;
        }
        .icon-success {
            color: var(--green-accent);
        }
        .icon-error {
            color: var(--red-danger);
        }
        .verification-container h1 {
            color: var(--header-primary);
            margin-bottom: 15px;
        }
        .verification-container p {
            color: var(--text-normal);
            font-size: 1.1rem;
            margin-bottom: 30px;
        }
        .login-btn {
            background-color: var(--brand-experiment);
            color: white;
            padding: 12px 30px;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
        }
        .login-btn:hover {
            background-color: var(--brand-hover);
        }
    </style>
</head>
<body class="auth-layout">
    <div class="verification-container">
        <% if (sucesso) { %>
            <i class="fas fa-check-circle verification-icon icon-success"></i>
            <h1>E-mail Verificado com Sucesso!</h1>
            <p>Sua conta foi ativada. Agora você já pode se conectar ao EsquizoCord.</p>
            <a href="/login" class="login-btn">Fazer Login</a>
        <% } else { %>
            <i class="fas fa-times-circle verification-icon icon-error"></i>
            <h1>Falha na Verificação</h1>
            <p><%= mensagem %></p>
            <a href="/cadastro" class="login-btn">Tentar Novamente</a>
        <% } %>
    </div>
</body>
</html>