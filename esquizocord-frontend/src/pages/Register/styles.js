// src/pages/Register/styles.js
import styled from 'styled-components';
import {
  LoginPageContainer,
  LoginBox,
  Title,
  FeedbackMessage,
  Form,
  Label,
  Input,
  Button,
  BackLink,
} from '../Login/styles';

export const RegisterPageContainer = styled(LoginPageContainer)``;
export const RegisterBox = styled(LoginBox)``;
export const RegisterTitle = styled(Title)``;
export const RegisterFeedbackMessage = styled(FeedbackMessage)``;
export const RegisterForm = styled(Form)``;
export const RegisterLabel = styled(Label)``;
export const RegisterInput = styled(Input)``;
export const RegisterButton = styled(Button)``;
export const RegisterBackLink = styled(BackLink)``;